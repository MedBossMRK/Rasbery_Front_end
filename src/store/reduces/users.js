import * as actionTypes from '../actions/authenticate/authActionsTypes';
import { updateObject } from '../utility.js';

const initialUsersState = new Array();


const reducer = (state = initialUsersState, action) => {
    switch (action.type) {

    }
    return state;
};

export default reducer;